name: Build precompiled NIFs
on:
  push:
    tags: ["v*"]

jobs:
  build_release:
    name: NIF ${{ matrix.nif }} - ${{ matrix.job.target }}
    runs-on: ${{ matrix.job.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        nif: ["2.17", "2.16", "2.15"]
        job:
          - { target: aarch64-apple-darwin, os: macos-14 }
          - { target: x86_64-apple-darwin, os: macos-14 }
          - { target: x86_64-unknown-linux-gnu, os: ubuntu-22.04 }
          - { target: aarch64-unknown-linux-gnu, os: ubuntu-22.04, use-cross: true }
          - { target: x86_64-unknown-linux-musl, os: ubuntu-22.04, use-cross: true }
          - { target: aarch64-unknown-linux-musl, os: ubuntu-22.04, use-cross: true }
          - { target: x86_64-pc-windows-msvc, os: windows-2022 }
    steps:
      - uses: actions/checkout@v4
      # The macos-14 runner is Apple Silicon (aarch64); building the x86_64
      # target there needs its std installed, which the action doesn't add.
      - name: Add x86_64 target for macOS cross-compile
        if: matrix.job.target == 'x86_64-apple-darwin'
        run: rustup target add x86_64-apple-darwin
      - uses: philss/rustler-precompiled-action@v1.1.5
        with:
          project-name: butteraugli_nif
          project-version: ${{ github.ref_name }}
          target: ${{ matrix.job.target }}
          nif-version: ${{ matrix.nif }}
          use-cross: ${{ matrix.job.use-cross }}
          project-dir: "native/butteraugli_nif"
